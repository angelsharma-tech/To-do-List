# To-do-List
A GUI-based To-Do List application built using Python and Tkinter. This application allows users to manage tasks efficiently with features like adding, updating, deleting, and marking tasks as complete. It also supports saving tasks to a file and loading them on startup for persistent task management.
